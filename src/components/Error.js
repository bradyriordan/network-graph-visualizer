import React from 'react'

const Error = () => {
    return (
        <p className="data-error scale-transition">Whoops! Looks like your JSON isn't formatted correctly. Fix the error and try again!</p>
    )
}

export default Error;